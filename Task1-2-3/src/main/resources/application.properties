spring.application.name=contactform
server.port=8081

# Active Spring Profile: 'mysql' for production/submission (requires running MySQL),
# or 'dev' for local testing (uses in-memory H2 database).
spring.profiles.active=mysql
